# Project Objective: Modernize SanskritSpellCheck to run on Python 3 + PHP 8, keep generating dictionary suspect-lists, and refine the detectors.

## ➡️ Next Steps (Queue)
- **Engineering hardening PR #45 — WAITING on external maintainer.** Review/merge
  [drdhaval2785/SanskritSpellCheck#45](https://github.com/drdhaval2785/SanskritSpellCheck/pull/45);
  GitHub may require maintainer approval before the new fork-PR workflow runs. PR #43 remains
  separate and unchanged. License selection is also a maintainer decision; no `LICENSE` was added.
- **H456 GRETIL upstream report — agent side DONE 10-07-2026 (Fable 5 `claude-fable-5`); MG sends the email.**
  All 124 bigram-screen loci hand-verified against the raw e-texts →
  [detectors/meter/GRETIL_UPSTREAM_REPORT.md](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/detectors/meter/GRETIL_UPSTREAM_REPORT.md):
  60 verified loci / 7 texts (systematic ḥ-for-vowel class in rāmāyaṇa-southern-2 + buddhist-stotras;
  agrya→agyra family ×7 across 2 files; Bhartṛhari 9 keying slips), 11 anomalous-no-fix-proposed,
  53 classified false positives (incl. two reversals: `'hbiṣekṣyati` IS a real e-text typo;
  `skmsauka`/`pvpadyā` are apparatus sigla, NOT corruption). Email drafted UNSENT at
  `Uprava/handoffs/OUTREACH_2026-07-10_gretil_etext_corrections.md`; GTD `@DO` = MG sends to
  gretil@sub.uni-goettingen.de, then flip to `@WAITING (Göttingen)`.
- **⭐ H454 GATED on ONE human sitting — vote the scan-verification sheet (10-07-2026, Fable 5 `claude-fable-5`).**
  Zero `y`-flipped rows exist in any of the 11 FILE-FIRST queues, so the batched-PR switchover
  (ruling D3) cannot generate a single change file yet. The gate is now clickable: open
  `review/sanskritspellcheck-filefirst-scanverify_109rows_review.html` locally (all 109 fileable
  rows — 92 PASS + 17 SCAN-FIRST — each with entry-body evidence, the verification note, and a
  Cologne scan deep-link; keyboard a/r/d), download
  `sanskritspellcheck-filefirst-scanverify_109rows_decisions.json`, then:
  `python corrections_draft/apply_scanverify_decisions.py <path-to-decisions.json> --apply`
  (flips approved rows `n`→`y`; rejected rows are listed for do-not-file routing). After that a
  Sonnet session resumes H454 step 2: `make_changefiles.py` per dict → xmllint → queue → batch 1
  report (NO csl-orig PR without MG authorization). Sheet regenerable via
  `python detectors/gen_scanverify_sheet.py`. The #447 follow-up comment is deferred until change
  files actually exist (posting "prepared" before verification would be false + noise).
- **A37 (orthographic-drift dater) data + hypothesis pass — DONE (2026-07-03, Fable 5 `claude-fable-5`).** Verified every A37 figure against the committed `ortho_drift/*_drift_summary.tsv` — German (PW 10.26/PWG 8.86/GRA 7.90/CCS 4.72/SCH 2.52 with th/c/ß splits), English (WIL 0.46→MW 0.01, 7 dicts at 0.00), Russian (KOSSOVICH 31,389/358.17, yat 8139/i 11106/hardsign 12125), Latin (BOP 0), French (BUR 0.31/STC 0.02) — **all EXACT**. `drift_dating.py` reproduces the §5 dating stats exactly (German ρ=−0.975/MAE 15yr, English ρ=−0.642/MAE 40yr, SCH mis-dated to 1896, 7-dict saturation). **A37 is the most reproducible paper of the batch** — generator committed + runs + matches; the only gap is the declared uncommitted Hunspell wordlist runtime dep (TSVs committed so downstream stats reproduce regardless). **Found + fixed a statistical overclaim:** the German dating p was reported as 0.005, but scipy's t-approximation is invalid at n=5 (below the exact minimum ~1/60). Added `exact_spearman_p()` to `drift_dating.py` (exact enumeration ≤8 pts, Monte-Carlo above): **German exact permutation p=0.033** (still sig, ~7× the t-approx), English p=0.016. Corrected ORTHO_DRIFT_FINDINGS.md + HYPOTHESES.md H5. **Article prose NOT touched.** ~~Queued A37 paper edits~~ ✅ **ALL DONE 03-07-2026 (Fable 5 `claude-fable-5`, H125 [PR #15](https://github.com/drdhaval2785/SanskritSpellCheck/pull/15) + follow-up)**: (1) §5 p-values → exact/permutation (DE 0.033, EN 0.016) ✓; (2) §4.4 "Ten English dictionaries" → "Fifteen (fourteen dated + held-out Apte)" ✓; (3) fine-dating framed on per-era composition, with a NEW committed era-flip figure `ortho_drift/drift_composition.png` (Figure 1) + `drift_dating.png` as Figure 2 ✓; (4) §2 Related work + full References WRITTEN from verified literature, Hunspell data-availability statement with snapshot identities (de_DE 103,756 / ropensci en_GB 56,571) ✓; plus "three-tier law"→three-regime stratification, Latin vitality confound, PW/PWG dependence (n=4 ρ=−1.00), denominator ¶, Kossovich §7 case-study framing. **A37 → 4/5, venue locked DSH; remaining: byline + MG read-through.** **A44 likewise revised same day** — the 02-07 review applied by TWO concurrent sessions ([PR #16](https://github.com/drdhaval2785/SanskritSpellCheck/pull/16) fuller rewrite + [PR #17](https://github.com/drdhaval2785/SanskritSpellCheck/pull/17) H127; reconciled, #16's paper text governs, addendum sync [PR #18](https://github.com/drdhaval2785/SanskritSpellCheck/pull/18)): IJL reframe + Appendix A, §4.5 verification fold-in per the committed tsv (97/12/11/1/1), collision fifth class, Tables 2a/2b per-class apparatus counts, per-phase model attribution, reproducibility split + eval.py. **A44 → 3/5; the 4/5 gate is the human IRR second annotator (GTD recruit) + MG References/read-through.**
0. **1-YEAR PLAN — REVISION 2 (10-07-2026, Opus 4.8 `claude-opus-4-8`): [ROADMAP_2026_2027.md](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/ROADMAP_2026_2027.md)**
   — re-interviewed after eight days diverged from revision 1. **Eight new MG rulings (D1–D8):** D1 Fable 5
   (`claude-fable-5`) = **judgment gates only** (papers, prior-art synthesis, triage adjudication); Sonnet 5
   classifies, Opus 4.8 confirms, mechanical build work is Sonnet's. D2 A44's human-IRR gate → **LLM
   second-annotator agreement study** (Cohen's κ), keeping the 31-12-2026 IJL date. D3 corrections switch from
   [CORRECTIONS#447](https://github.com/sanskrit-lexicon/CORRECTIONS/issues/447) (8 days, zero comments) to
   **monthly consolidated PRs**. D4 the over-delivered meter track **folds into A47** as a joint sound/metre
   paper; D5 anuprāsa stays Q1 2027, so **A47 submits Q3 2027**, past the horizon. D6 **all four products
   kept** (Zenodo → PyPI → web app → Cologne integration). D7 union-across-runs cut to **three dicts** and
   measured, not assumed. D8 GRETIL e-text typos **reported upstream immediately**.
   **Four handoffs minted:** [H452](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H452-Fable_SanskritSpellCheck_prior-art-scan_10.07.26.md)
   (prior-art scan — the lead task) · [H453](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H453-Fable_SanskritSpellCheck_a44-llm-second-annotator-irr_10.07.26.md)
   (A44 κ study) · [H454](https://github.com/gasyoun/Uprava/blob/main/handoffs/H454-Sonnet_SanskritSpellCheck_corrections-batched-pr-switchover_10.07.26.md)
   (batched-PR switchover) · [H456](https://github.com/gasyoun/Uprava/blob/main/handoffs/archive/H456-Sonnet_SanskritSpellCheck_gretil-typo-report-upstream_10.07.26.md)
   (GRETIL typo report). Revision-1 detail below is history, not instructions:
0a. **Revision 1 (superseded 10-07-2026): [ROADMAP_2026_2027.md](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/ROADMAP_2026_2027.md)**
   — MG interview decisions: file the 122 NOW **without the OCR gate** (umbrella issue, SHS pilot
   section first, MG posts); ≥300 corrections filed by 30-06-2027; A44→IJL + A37→DH both submitted
   Q4 2026; productize ×4 (PyPI, web app new GH-Pages repo, Cologne integration, Zenodo dataset);
   pilots = union-across-runs recall + SHS body deterministic + MW-citation LLM triage (generous
   budget); monthly hybrid loop (CI detects, agent triages). **Fable 5 (`claude-fable-5`) for judge
   gates until 08-07-2026**, then Sonnet 5 (`claude-sonnet-5`)/Opus 4.8. Q3 sequence = roadmap §Q3 items 1–7; item 2
   (umbrella issue) is the LEAD task — H008-Opus_SanskritSpellCheck_NEXT_23.06.26 Task 1 applies minus the OCR steps.
1. **Headword triage — ALL 33/33 done** (index: `corrections_draft/README.md`). No agent work left here.
   HUMAN payoff: verify the FILE-FIRST queues against the scans, biggest first — **SHS 37** (high-yield
   outlier), YAT 27, ACC 22, PWG 12, MCI 10, MW 4, SKD 3, WIL 3, PW 2, VCP 1, GST 1 — flip `n`→`y`, file
   to CORRECTIONS. Do NOT bulk-apply tier A. **122 fileable across 11 dicts.** ⚠️ Re-runs are stochastic;
   don't blindly overwrite a committed package.
2. **PD done on source 1; 2nd source OPTIONAL (refines only).** PD = Deccan College Encyclopaedic Dict
   (en, 55MB/107630 entries, CC BY-NC-SA), staged at `external_src/pd/` (gitignored) via
   `detectors/get_external_source.py PD`; resolved by `triage_util.source_file()` (external_src override
   → csl-orig fallback). Triaged: 0 fileable / 116 do-not-file (richest apparatus of any dict). To add
   PD's 2nd source later: register it as a 2nd tuple in `get_external_source.py` SOURCES['PD'], re-stage,
   `/dict-triage PD`, then fold do-not-file via gen_do_not_file_suppress + eval (FP=0) — refines the
   116-entry list only.
3. **Ortho-drift study — ALL 5 LANGUAGES DONE** (`ORTHO_DRIFT_ROADMAP.md`; tool `detectors/ortho_drift.py`,
   profile-driven de/fr/la/ru/en via LANG_OF+PROFILES). German cluster + SCH-1928 control + residual
   classify (de map **2823 forms**); French (BUR/STC); Latin (BOP = 0, control); Russian (Kossovich,
   wordlist-free 1918); **English 10-dict (WIL 0.57 max → MW 0.01)**. Headline: drift = f(reform TYPE),
   legislated (RU 358, DE 10) ≫ convention (EN ≤0.57, FR ≤0.31) ≫ none (LA 0). **External-pair merge
   DONE: `detectors/merge_reform_pairs.py` (dic-validated ingest); 14 documented 1901/1996 pairs from
   German Wikipedia merged → de 2809→2823.** ⚠️ WebFetch DOES reach the web (pip PyPI timeout was its
   own issue) but can't bulk-download DTA/RIDGES corpora → drop a local export / row-list URL to merge
   the long tail. Optional remaining: 4 modern-EN dicts (BHS/IEG/PE/VEI) as a within-EN recency control.
   ⚠️ Hunspell de_DE/fr_FR/en_GB = Adobe-bundled LOCAL DEP (`$ORTHO_<L>_DIC`), NOT committed.
4. Phase 2 external-dep (built, gated on installs): OCR triage (`pip install pytesseract` + tesseract
   `san`), full DCS sqlite (VisualDCS release). **GRETIL download — DONE, see H251 below.**
- **H251 batch chandas (meter) validator — DONE (07-07-2026, Sonnet 5 `claude-sonnet-5`),
  re-prioritized early from its original Q1 2027 slot by MG same-session.** Built the full
  pipeline per [`detectors/meter/README.md`](detectors/meter/README.md): GRETIL Kavya-section
  walker (57 texts, gitignored raw corpus — CC BY-NC-SA), three-way skrutable+chanda+vidyut-chandas
  meter identifier + verdict, `vidyut.cheda`-based word→headword bridge, new `meter_check` flagger
  wired into `run_all.py`'s `DETECTORS` (not in `HIGH_PRECISION` — ranking-nudge only, per the
  locked decision). **Full-corpus index built: 25,824 verses parsed (9,005 blocks had no locus tag,
  184 blocks were prose not verse — see below), 25,705 actually scored → clean=15,772 (61.4%),
  review=6,357 (24.7%), suspect=3,695 (14.4%).** `meter_check` bridges 10,052 non-clean verses to
  7,925 distinct headwords (`detectors/meter_suspects.txt`, gitignored/regenerable like the other
  `*_suspects.txt`). `run_all.py` verified end-to-end with all 7 detectors (22,126 unified
  candidates, tiers A=5392/B=4713/C=12021).
  **Three real bugs found + fixed during the build (all now in the committed code):**
  (1) skrutable's bracketed meter-variant detail (`vaṁśastha [12: jtjr]`) wasn't stripped before
  name comparison, and chanda's `=`-joined meter-synonym groups were compared as whole strings
  instead of being flattened — both caused false "review" disagreements on genuinely clean verses
  (fixed in `meter_ident.py`'s `_norm_name`/`_flatten_chanda_names`).
  (2) 5 of the 57 "Kavya" GRETIL texts (incl. Daṇḍin's *Daśakumāracarita*, a PROSE romance) were
  being walked as if every locus-tagged paragraph were a verse — some blocks ran to 19,000+ chars,
  producing meaningless verdicts and sending skrutable's `resplit_lite` into a hang. Fixed with a
  `MAX_VERSE_CHARS=500` prose filter in `gretil_walker.py` (99th-percentile real verse is 359 chars;
  184 blocks across 5 files correctly excluded as prose, logged not silently dropped).
  (3) `vidyut-chandas` (Rust) panicked (`index out of bounds`) on a handful of pathological inputs;
  `pyo3_runtime.PanicException` does not subclass `Exception`, so a plain `except Exception` let it
  crash the whole batch run twice near the end. Fixed with `except BaseException` in
  `identify_vidyut` (vidyut is a corroborating vote only per the locked decision — degrading to "no
  opinion" on a panic is the correct behavior, not a data-loss risk).
  **Recalibrated by H277 (07-07-2026, Opus 4.8 `claude-opus-4-8`) — see entry below.** The 39%
  non-clean rate flagged in this build was over-flagging valid poetry (skrutable `is_perfect=False`
  on recognized vipulā/upajāti varieties + a chanda name-mismatch cross-check firing on those same
  varieties), not finding corruption; H277 cut it to 15.9% and made `meter_check` a corroboration
  nudge in `run_all.py` rather than a tier-promoting detector peer.
- **H277 meter-validator recalibration — DONE (07-07-2026, Opus 4.8 `claude-opus-4-8`).**
  Follow-on to H251 after MG: "39% is too high, no human will check so many." Root-caused and fixed
  the over-flagging in [`detectors/meter/meter_ident.py`](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/detectors/meter/meter_ident.py)'s
  `verdict()`: the only per-verse signals kept are a **localized broken syllable** (non-empty
  `problem_padas`) or a **total scan failure** (`_skrutable_scanned()` — empty / `na kiṃcid
  adhyavasitam` label); a recognized irregular variety with neither is now `clean`, and a bare
  chanda/vidyut name-mismatch no longer downgrades a clean-scanned verse. **Non-clean 39.1% → 15.9%**
  (clean 21,714 / review 1,438 / suspect 2,672). Verdicts recomputed from the stored raw tool outputs
  in the existing jsonl via the new
  [`reprocess_verdicts.py`](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/detectors/meter/reprocess_verdicts.py)
  (no ~1hr corpus rebuild). Plus two precision changes: a **DCS-rarity gate** in
  [`meter_check.py`](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/detectors/meter_check.py)
  (`RARE_BAND=2` → 7,925 → 1,719 rare headwords) and **corroboration-nudge-only** consumption in
  [`run_all.py`](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/detectors/run_all.py)
  (`meter_check` no longer joins `c.detectors`, so it can't coincidence-promote to tier A — 23 such
  promotions removed; candidates 22,126 → 14,752, tier A 5,392 → 5,369, 46 rows carry a legit
  `meter=` nudge, 0 meter-sole in tier A). Hand-check confirmed the reclassified `adhikākṣarā`
  verses are prose-intrusion artifacts (`X uvāca:` speaker tags bundled by the walker), not typos.
  Full writeup: [`detectors/meter/README.md`](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/detectors/meter/README.md)
  "Recalibration (H277)".
- **H277-b follow-ups — DONE (07-07-2026, Opus 4.8 `claude-opus-4-8`).** Four MG-requested additions,
  all landed in one clean corpus rebuild (25,559 unique verse records; non-clean stable at **15.9%** —
  clean 21,505 / review 1,380 / suspect 2,674): (1) **walker prose-intrusion strip**
  ([`gretil_walker.py`](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/detectors/meter/gretil_walker.py)
  `_strip_prose_prefix`) removes leading `X uvāca:` speaker tags / `… 'dhyāyaḥ`/`… sargaḥ` headings
  (39 verses; anuṣṭubh spurious `adhikākṣarā` 592→558); (2) **pāda-segmented text stored** in every
  record (`skrutable.padas`/`pada_weights`) for future per-pāda bridge localization; (3) **vipulā/meter
  variety census** ([`variety_stats.py`](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/detectors/meter/variety_stats.py)
  → [`METER_VARIETY_STATS.md`](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/detectors/meter/METER_VARIETY_STATS.md),
  logged as valid poetic variation, NOT a suspect list: anuṣṭubh 48.7% of the corpus; pathyā 78.2% of
  anuṣṭubh pāda-pairs, then na-/ma-/bha-/ra-vipulā + asamīcīnā); (4) **smaller high-precision sibling**
  `meter_suspects_strong.txt` (suspect + DCS band ≤ 1 → 700 rows) beside the full 1,703-headword
  `meter_suspects.txt`. Also fixed a latent `build_meter_index.py` dup-write bug (never recorded a
  just-written key → 172 duplicate-locus records; now `done.add`s each). One operational mishap during
  the rebuild (a stray first process co-wrote the jsonl → byte corruption) was caught and cleanly
  recovered by salvaging the 16,984 valid records as a resume base + finishing the gap. Full writeup:
  [`README.md`](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/detectors/meter/README.md)
  "H277-b follow-ups".
5. ~~Calibration: tier-C ~913 historically-real pairs (boost single-detector recoveries w/ corpus/DCS
   signal)~~ **STRUCK 29-07-2026 (H1878) — already attempted, NEGATIVE RESULT, do not re-queue.** See
   "✅ Completed" → "Tier-C calibration (Task 2) — NEGATIVE RESULT": corpus+confusion tier-C→B promotion
   surfaces real minimal pairs as false-positive typos (patra/pAtra, vata/rAtrI); tier promotion was
   rejected and a CORROB_* score nudge kept instead. **⚠️ DON'T re-attempt corpus-based tier-C
   promotion — it's a ceiling** (the body-grounded triage is the arbiter). Remaining live items: record
   SanskritSpellCheck as a sanskrit-util consumer in SHARED_CODE.md; low: order_check source-order list,
   codecs.open→open(encoding=) cleanup, ported sanhw1/2.py vs server scans.

## 🚧 Current Work-In-Progress (WIP)
- **H825 (ACL uplift, ruling D9/D10/D11) — build DONE 12-07-2026 (Sonnet 5 `claude-sonnet-5`
  mechanical + Fable 5 `claude-fable-5` register pass), one real gate left open.**
  `detectors/eval.py` now reports detection-level F0.5/FPR/harm against a new held-out
  `detectors/gold_corrections.tsv` (build_gold_set.py); measured result: the raw detectors flag
  77–100% of the 13 HARM (EDITORIAL/DNF/DROP) rows, direct evidence for A44's own do-not-file
  thesis. `detectors/irr_cross_family.py` (cross-family blind annotator, non-Anthropic judge) is
  built and dry-run verified but **could not actually run — no LLM_API_KEY on this host**; A44's
  κ=0.336/0.663 stay LLM-only inter-rater figures. **Next:** (a) HUMAN — a human should decide
  whether to configure a DeepSeek/OpenAI-compatible key and run
  `python detectors/irr_cross_family.py`, and separately whether to produce the ~30-row
  human-labelled seed anchor flagged in
  [corrections_draft/irr/HUMAN_ANCHOR_NEEDED.md](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/corrections_draft/irr/HUMAN_ANCHOR_NEEDED.md)
  before treating either kappa as submission-ready; (b) once resolved, re-run
  `detectors/irr_agreement.py` and fold the real cross-family κ into §4.6.
- **UMBRELLA ISSUE POSTED (02-07-2026):**
  [sanskrit-lexicon/CORRECTIONS#447](https://github.com/sanskrit-lexicon/CORRECTIONS/issues/447)
  — 92 proposed + 17 scan-first + 11 editorial across 11 dicts, from the verified queue
  ([file_first_verified.tsv](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/corrections_draft/file_first_verified.tsv);
  source of record: [CORRECTIONS_umbrella_issue.md](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/corrections_draft/CORRECTIONS_umbrella_issue.md)).
  **Next:** (a) HUMAN — M.G. scan-verifies SHS → YAT → ACC and answers maintainer feedback on
  #447; (b) AGENT — on maintainer request, generate per-dict draft `updateByLine` change-files
  (`detectors/make_changefiles.py`, prep-not-submission); (c) applying anything to `csl-orig`
  stays human-gated per the canonical workflow.
- **Prioritized handoff roadmap** ([H008-Opus_SanskritSpellCheck_NEXT_23.06.26.md](H008-Opus_SanskritSpellCheck_NEXT_23.06.26.md)).
  4 ranked tasks w/ paste-ready briefs: (1) LEAD = draft the CORRECTIONS **umbrella issue** for the
  122 FILE-FIRST typos, **OCR-prefiltered** (ocr_verify.py, needs tesseract+`san`), **pilot SHS then
  scale**, human posts; (2) tier-C calibration; (3) ortho-drift recency (drop en_GB Hunspell at
  `$ORTHO_EN_DIC`); (4) DTA/RIDGES merge (drop export). Also fixed `make_changefiles.py` (skip
  `;`/`#` sf-comment lines + alnum dict-code guard) so FILE-FIRST sf feeds the changefile prep cleanly.

## 🧠 Dev Notes & Hypotheses (Bugs, ideas, context)
- **CONVENTION (user, 2026-06-16): every dictionary triaged MUST emit a separate
  `<DICT>_wrong_readings.txt`** — the standing do-not-file list of spellings the dict
  documents on purpose (wrong-reading `w.r.` apparatus, `v.l.`, in-composition/sandhi,
  cross-references, other grammatical/Vedic notes), grouped by sub-type. `triage_synthesize.py`
  generates it automatically from the INTENTIONAL bucket. MW: 630 (w.r. 45, v.l. 54,
  in-comp. 105, xref 204, other 222). Use as a suppression list so future runs never re-flag them.
- Runtimes: PHP 8.2.12 at `C:\xampp\php\php.exe` (not on PATH), Python 3.14.4, lxml 6.1.0.
- **Tail-drop bug (now fixed)**: the check loop was `for ($j<count($file1))` over `array_diff()` output, whose keys are gappy → it stopped at the survivor count and never tested survivors past it (the alphabet tail). `foreach ($file1 as $j=>$value)` fixes both the gaps (warnings) and the coverage. Purely additive: VCP 6856→7411 (+555), all new ones s…/h…; 0 lines lost.
- **Re-run vs committed**: fresh MW/PW/PWG = 110/183/256 vs committed 1705/1853/1984. Head of alphabet was corrected upstream since 2017; the tail counts are NEW (never tested before the fix). Not a regression.
- `sanhw*.py` need sibling `<CODE>Scan/<year>/pywork` trees absent locally → ported-for-correctness, not runtime-verified here.
- Historical `AllvsMW/PW/PWG/VCP` left as 2017 point-in-time runs (audit trail) — not overwritten.

## ✅ Completed (Recent only)
- **H2407 A44 *IJL* submission pack DONE (10-08-2026, Fable 5 `claude-fable-5`).**
  [PR #3](https://github.com/gasyoun/SanskritSpellCheck/pull/3) +
  [PR #4](https://github.com/gasyoun/SanskritSpellCheck/pull/4) merged,
  [v1.59.0](https://github.com/gasyoun/SanskritSpellCheck/releases/tag/v1.59.0) cut:
  [papers/A44_cover_letter.md](https://github.com/gasyoun/SanskritSpellCheck/blob/master/papers/A44_cover_letter.md),
  [papers/A44_submission_checklist.md](https://github.com/gasyoun/SanskritSpellCheck/blob/master/papers/A44_submission_checklist.md)
  (16 venue requirements, OUP general instructions fetched 10-08-2026),
  [papers/A44_checklist.md](https://github.com/gasyoun/SanskritSpellCheck/blob/master/papers/A44_checklist.md)
  (ARR Responsible NLP filled),
  [CITATION.cff](https://github.com/gasyoun/SanskritSpellCheck/blob/master/CITATION.cff) — the repo's first — and
  [papers/validate_a44_pack.py](https://github.com/gasyoun/SanskritSpellCheck/blob/master/papers/validate_a44_pack.py) as the guard.
  - **Scope correction:** the handoff title said "camera-ready", but A44 has no
    acceptance, and `/paper-camera-ready` Phase 0 gates explicitly on a real accept
    decision. A submission pack is the correct artifact; a camera-ready pass would have
    been wrong work.
  - **Blocker for a human:** *IJL* requires funding sources named in the manuscript and
    A44 has none. Four further requirements (word limit, abstract length, keyword count,
    anonymous-review question) are **UNVERIFIED** — they live in OUP's gated Author Pack,
    so they are recorded as ❓ rather than guessed. Measured: ~6,475 words, ~331-word
    abstract (over the usual 250 cap), no keywords line, and a named byline plus
    self-citing repo URLs that anonymous review would turn into disclosure defects.
  - **Licence gap is now on the record twice** (ARR item B2 + `CITATION.cff`): this repo
    declares no software licence, and neither do the created do-not-file artifacts that
    the paper offers for reuse. `CITATION.cff` therefore ships with **no `license:` key
    and no DOI** — inventing either would be a fabricated claim, and the selftest asserts
    they stay absent.
- **H1533 monthly detection-loop GitHub Actions cron DONE (27-07-2026, Sonnet 5
  `claude-sonnet-5`).** Roadmap Q4 item 5:
  [.github/workflows/monthly-detection-loop.yml](.github/workflows/monthly-detection-loop.yml)
  (monthly cron + `workflow_dispatch`) runs `run_all.py`'s detector suite against
  `sanhw1.txt`, then `detectors/monthly_loop.py` diffs the tier-A candidates against a
  committed baseline ([detectors/monthly_loop/tier_a_baseline.txt](detectors/monthly_loop/tier_a_baseline.txt))
  to surface what's NEW since the last cycle, writes a dated report
  (`detectors/monthly_loop/reports/<YYYY-MM>.md`), uploads run artifacts every cycle, and
  opens/updates a PR with the refreshed baseline + report only on a genuine delta (quiet
  months make no noise). **Found + fixed a real bug while building it:** PR #45's cache
  hardening treated any zero-byte detector output as a crash, but `meter_check` (no
  offline corpus index on a fresh clone) and `tied_field_check` (currently zero real
  disagreements) both legitimately produce one — `run_all.py --rerun` hard-failed on
  every stock checkout until fixed to trust `returncode` alone (regression test added in
  `tests/test_run_all_cache.py`). First cycle seeded the baseline at 5,369 tier-A
  candidates (the full current backlog, expected for a first run — see
  `detectors/monthly_loop/reports/2026-07.md`). **`workflow_dispatch` verified on master,
  two rounds:** round 1 failed (`ModuleNotFoundError: sanskrit_util` — the workflow never
  installed `requirements.txt`, so `meter_check.py`'s transcoder import had nothing to
  import on the runner; fixed in [PR #55](https://github.com/drdhaval2785/SanskritSpellCheck/pull/55)).
  Round 2 succeeded end-to-end including the delta-PR step, and correctly surfaced 2
  genuinely new tier-A candidates the local baseline-seeding run had missed (that run's
  machine happened to have the optional cross-repo union-attestation file, demoting them
  A→B; the CI runner doesn't, so neither will any future scheduled cycle) — `JarA -> jarA`
  and `aramya -> araRya`, merged via [PR #56](https://github.com/drdhaval2785/SanskritSpellCheck/pull/56).
  Those two are now baselined, not filed — an actual triage/filing decision on them is a
  separate future session's job, not part of this infra handoff.
- **Engineering remediation DELIVERED (19-07-2026, GPT-5 `gpt-5`) —
  [PR #45](https://github.com/drdhaval2785/SanskritSpellCheck/pull/45).** From fresh
  `origin/master`, independently of unchanged PR #43: repaired executable unified/campaign
  review JSON and dictionary-scoped exports; enforced the four-argument PHP CLI; replaced
  implicit reuse with a SHA-256 cache manifest, atomic full reruns, and explicit stale escape;
  pinned core/dev/analysis dependencies; made `sanskrit-util` package-first with sibling
  fallback; added Vidyut data precedence, Python/PHP CI, pip Dependabot, and factual
  installation/missing-license documentation. Verified 21 pytest tests, all 26 legacy checks,
  Ruff F841, all 34 PHP syntax checks, real installed-package portability, and real-browser
  voting/persistence/filter/keyboard/download/campaign-scope/viewport behavior. No generated
  detector outputs or caches remain; no license was selected.
- **H827 tied-field cross-encoding consistency detector DONE (13-07-2026, Sonnet 5
  `claude-sonnet-5`).** New 11th detector family
  [detectors/tied_field_check.py](detectors/tied_field_check.py) — the Bloodgood & Strauss
  (arXiv 1602.07807) "tied-field consistency" shape: round-trips every `sanhw1.txt` headword
  through SLP1→Devanāgarī→SLP1 and SLP1→IAST→SLP1 via the shared `sanskrit-util` package
  (3 new thin wrappers in `slp1util.py`). Wired into `run_all.py` as a high-precision
  flagger; `eval.py` filing gate stays PASS. **Full run, 431,596 lines / 431,568 unique
  in-alphabet headwords: 0 unsuppressed disagreements** — every mismatch fully explained by
  two documented transcoder asymmetries (candrabindu/avagraha via Devanāgarī, 12 instances;
  aspirate/diphthong digraph ambiguity via IAST, 100 instances), zero unexplained residual.
  Honest negative finding on error discovery (the transcoder is round-trip clean at scale)
  + positive finding on methodology (detector correctly separates real defects from
  normalization axes — see H8 in [docs/HYPOTHESES.md](docs/HYPOTHESES.md)). No corrections
  filed (nothing to file).
- **H826 A37 ACL uplift DONE (13-07-2026, Sonnet 5 `claude-sonnet-5`).** S-curve
  exo/endo fit + SemEval-2015 DTE benchmark + LChange companion, per ruling D15.
  [detectors/drift_dating.py](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/detectors/drift_dating.py)
  extended with `fit_scurve()` (Ghanbarnejad et al. 2014, arXiv:1406.4498) and
  `dte_bands()` (SemEval-2015 S15-2147/S15-2148); results in
  [docs/ORTHO_DRIFT_FINDINGS.md](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/docs/ORTHO_DRIFT_FINDINGS.md)
  §O7a/§O7b. **Headline finding is a negative one, reported honestly rather than
  forced**: the naive cross-sectional S-curve proxy INVERTS the expected exo/endo
  mechanism ordering (English/convention fits "abrupt" 9.7yr; German/legislated fits
  "gradual" 50.2yr) — diagnosed as zero-censoring (English) and sparse edition-spacing
  around the 1901 reform date (German), not a real mechanism signal. A37 paper §2/§4.8/§5
  updated with 5 new verified citations; new
  [papers/A37_lchange_companion.md](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/papers/A37_lchange_companion.md)
  ("When the S-curve Lies") isolates the inversion as a standalone LChange short-paper
  draft with a 4-point diagnostic checklist. No detector-logic change (paper
  scholarship only). PR: (see changelog/release for link).
- **H828 web-suggester SPEC DONE (12-07-2026, Opus 4.8 `claude-opus-4-8`; intended executor Fable 5, run at MG's direction).**
  [docs/WEB_SUGGESTER_SPEC.md](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/docs/WEB_SUGGESTER_SPEC.md)
  — design spec (no build, ruling D13) for the Q1-2027 web spellchecker's suggestion engine:
  [Oflazer 1996](https://aclanthology.org/J96-1003.pdf) error-tolerant FST traversal (cut-off edit
  distance) over the 431,596-headword union trie + the vendored Vidyut kosha/stems
  (validator→generator), ranked by a [Brill-Moore 2000](https://aclanthology.org/P00-1037/) noisy
  channel (string-edit channel × DCS band prior × measured
  [confusion_weights.json](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/detectors/confusion_weights.json)),
  with the do-not-file catalogue (2,297) ∪ nochange.txt (30,887) as a variant-aware accept list.
  Reuse map, 4 differentiators, licensing guards (Vidyut MIT embeddable; do NOT ingest LibreOffice/GPL
  wordlists), ISCLS demo hook (D12), 8-item build breakdown (B2/B3 = the hard pieces), 5 parked open
  questions. Merged [#34](https://github.com/drdhaval2785/SanskritSpellCheck/pull/34), released
  [v1.52.0](https://github.com/drdhaval2785/SanskritSpellCheck/releases/tag/v1.52.0). Build is a
  future handoff-set, deferred post-30-06-2027 so it does not compete with the ≥300-corrections north-star.
- **H678 A37 author-voice pass DONE (11-07-2026, Fable 5 `claude-fable-5`, /paper-author-pass).**
  13 register/alignment calls on [papers/A37_ortho_drift_paper.md](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/papers/A37_ortho_drift_paper.md)
  (abstract grammar + Cologne-scope alignment, O-code heading strips, de-agentification;
  numbers/citations verified token-identical to origin/master, 11 citations checked both
  directions — no A44-style dangling ref). Residual human calls in
  [papers/SIGNOFF_A37_author_pass.md](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/papers/SIGNOFF_A37_author_pass.md);
  byline gate discharged (canonical identity verbatim). GTD @DO = MG read-and-sign
  (~30 min), then bump A37 → 5/5 in ARTICLES.md.
- **H453 A44 blind LLM second-annotator IRR study DONE (10-07-2026, Fable 5 `claude-fable-5`
  orchestration + adjudication; annotator B = Opus 4.8 `claude-opus-4-8` ×10 blind agents;
  annotator A = the committed 02-07 pass, Sonnet 5 `claude-sonnet-5` + Fable 5 adjudication).**
  Ruling D2 executed: all 122 [file_first_verified.tsv](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/corrections_draft/file_first_verified.tsv)
  rows re-annotated blind (evidence = entry bodies under both spellings only, built by
  [detectors/irr_build_inputs.py](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/detectors/irr_build_inputs.py)).
  **κ = 0.336 five-way (po 59.0%), binary defect-recognition 121/122 = 99.2% (κ 0.663)** —
  exact rational arithmetic via [detectors/irr_agreement.py](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/detectors/irr_agreement.py),
  artifacts in [corrections_draft/irr/](https://github.com/drdhaval2785/SanskritSpellCheck/tree/master/corrections_draft/irr).
  Disagreement taxonomy: 33 collision-threshold + 16 evidence-threshold policy divergences +
  1 reversal, zero evidence misreads; blind pass reproduced MG's 02-07 PASS→SCAN-FIRST audit
  on 4/5 SHS rows. Paper: new §4.6, limitations rewritten honestly, **stale 97/12 counts fixed
  to the artifact's 92/17** (MG audit commit `ce8f4f7` governs; the handoff guard had it
  backwards). A44 3/5 → 4/5; remaining gates = MG References pass + read-through. κ computed
  once, first run, no prompt iteration (anti-p-hacking guard honored).
- **H452 prior-art scan DONE (10-07-2026, Fable 5 `claude-fable-5`)** — roadmap Q3 item 1,
  the lead task. New [docs/PRIOR_ART.md](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/docs/PRIOR_ART.md):
  12+ tools with the five-column verdict (approach · data · license · maintenance ·
  reuse-or-avoid), three parallel research agents + main-session verification, every claim
  fetch-backed. Headlines: (1) the netlify spellchecker M.G. named = **Prasanna S., ICON 2022**
  ([2022.icon-main.35](https://aclanthology.org/2022.icon-main.35/)) — the paper's own footnote
  links the site; 37,058-entry Paninian word-and-paradigm Hunspell dic; source never published,
  dormant since ~2022. (2) **LibreOffice bundles a 543,758-entry `sa_IN` Hunspell pair since
  01-2025** (Shantanu Oak, web-scraped wordlist, license formally unsettled in-tree — GPL-2
  COPYING added then reverted 05-2025) — do NOT ingest until settled; use as eval baseline.
  (3) No maintained flag-and-suggest Sanskrit spellchecker exists; suggestion generation
  against a validated lexicon is unoccupied — [COLOGNE#91](https://github.com/sanskrit-lexicon/COLOGNE/issues/91)
  open since 2016 is the demand signal. (4) **A44 mis-citation found+fixed**: its "ISCLS 2024
  contextual spellchecker" reference doesn't exist in that volume; corrected to Prasanna 2022
  in §2 + References. A37 §2 got the object-language delimiting paragraph. Roadmap Q3 item 1
  ticked. Both papers' related-work sections now cite the scan.
- **H289 Phase 2–3 DONE (07-07-2026, Opus 4.8 `claude-opus-4-8`)** — extended the meter QA
  to 4 more genre-diverse GRETIL sections (Purāṇa/Epic/Subhāṣita-anthology/verse-śāstra,
  each a 1500-block sample). New tooling in `detectors/meter/`: `fetch_gretil.py` (fetch+
  sample), `build_section_dataset.py` → `SECTIONS_DATASET.md` (Phase-2 cross-section
  dataset — non-clean% tracks metrical ornateness not corruption: Manu 0.1%, Purāṇa 0.9%,
  Epic 3.7%, Vidyākara 11%, Kāvya 15.9%; no genre spiked), `multisection_error_candidates.py`
  → `MULTISECTION_ERROR_CANDIDATES.md` (Phase-3 stream 1: 20 tier-A/B candidates meter-nudged
  by the enlarged Kāvya+sections index, 3 new-section-only → `/review-sheet`),
  `ngram_corpus_check.py` → `GRETIL_TEXT_TYPOS.md` + `ngram_typos_*.tsv` (Phase-3 stream 2:
  MW∩PW-bigram checker on IAST→SLP1 running text; caught a systematic ṝ→ḥ encoding
  corruption in the Rāmāyaṇa e-text — report to GRETIL, not CORRECTIONS). Meter stays a
  corroboration nudge (H277 invariant). Raw corpus + `meter_verdicts_*`/`meter_suspects_all*`
  gitignored; deliverable MDs/TSVs committed. PR to follow.
- **Chandas-checker tiny pilot DONE (06-07-2026, Sonnet 5 `claude-sonnet-5`)** — per
  [CHANDAS_ANUPRASA_PRIOR_ART.md §2](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/docs/CHANDAS_ANUPRASA_PRIOR_ART.md)'s
  MG-scoped pilot: ran `skrutable` + `chanda` on all 111 verses of GRETIL's Meghadūta
  (Vallabhadeva/Hultzsch), diffed verdicts, hand-checked both residual anomalies against
  a second independent GRETIL rendering. Full writeup + proposed detector output format
  in [§4 of the same doc](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/docs/CHANDAS_ANUPRASA_PRIOR_ART.md#4-pilot-results-06-07-2026-sonnet-5-claude-sonnet-5).
  **Key finding:** chanda's strict/exact matcher fails on ~72% of genuinely correct
  verses (systematic sandhi-resyllabification + padānta-guru gaps) — a validator must
  treat exact-OR-fuzzy(cost≤2) as its clean signal, never exact-only, or it will
  false-flag most undamaged text. skrutable correctly caught one genuine hypermetric
  pāda (v62, confirmed not a transcription artifact). Per MG's timing ruling, **parked
  until Q1 2027** — no further batch/GRETIL-walker work queued now. Pilot scripts were
  throwaway (not committed); reproducible from the writeup.
- **Fable-window CLOSED OUT for this repo (2026-07-02, Fable 5 `claude-fable-5`):** all
  Fable-gated items done — (a) 28 verification flags adjudicated; (b) both papers fact-check-
  adjudicated (6 fixes); (c) post-posting judge spot-check 10/10 PASS sample vs csl-orig
  (VERIFICATION_2026_07.md §spot-check; side-flag `YAT zazWimatta`); (d) referee reviews
  written: [papers/A37_review_fable5.md](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/papers/A37_review_fable5.md)
  + [papers/A44_review_fable5.md](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/papers/A44_review_fable5.md)
  (prioritized fix lists; ~2 sessions each → 4/5; A44 IRR = human gate). Everything remaining
  (umbrella follow-ups, paper revisions, pilots, monthly loop) is Sonnet 5/Opus 4.8 work by design.
- **Fable-window verification gate (2026-07-02):** roadmap §Q3 item 1 DONE. (a) Both papers
  fact-checked (Sonnet 5 `claude-sonnet-5` agents — ⚠️ initially misattributed as 4.6 from a stale
  alias mapping; corrected 02-07 — ~75 claims verified) → 6 real errors fixed
  (A44 BHS-294 omission; A37 five: 56,571-stem dic, Cologne 33, "up to three orders" w/ 5× floor,
  1832–2009 span, nine+Apte). (b) All 122 FILE-FIRST rows source-verified (4 Sonnet checkers,
  Fable 5 `claude-fable-5` adjudicated all 28 flags) → **97 PASS / 12 SCAN-FIRST / 11 EDITORIAL
  (collision pairs — new category) / 1 DNF (RiS ṇopadeśa) / 1 DROP (already fixed upstream)** →
  `corrections_draft/file_first_verified.tsv` + `VERIFICATION_2026_07.md`. MW's "4 of 1,954" is
  honestly 2 scan-first + 2 editorial. Hubs updated: 2 findings → SanskritLexicography/FINDINGS
  (collision ~9%; queue decay 0.8%/wk), 1 → Uprava/FINDINGS (checkers string-match & under-weight
  morphology — judge pass required); do-not-file corpus + reform maps registered as proposed feeds
  in PROJECT_INTERLINKS. ⚠️ Checker lesson: never exact-substring readme evidence (templates +
  U+00B0/U+02DA trap); exceptionless grammar (ṣatva/ṇatva/voḍhavya) IS evidence.
- **1-year roadmap + changelog restore (2026-07-02, Fable 5 `claude-fable-5`):** authored
  [ROADMAP_2026_2027.md](https://github.com/drdhaval2785/SanskritSpellCheck/blob/master/ROADMAP_2026_2027.md)
  from a 3-round MG interview (all decisions in the file's locked-decisions table); marked ROADMAP.md
  superseded as forward plan. **Restored changelog.md** — commit `78cc59d` (30-06) had wiped the
  1.0.0–1.40.0 history (967 lines) with a generic 0.1.0 stub; restored from `4a80062`, promoted
  [Unreleased] → **[1.41.0]**, tagged + GitHub release (repo's first).
- **Union-attestation signal (2026-06-26, branch `feat/union-attestation`):** wires the
  PROJECT_INTERLINKS "union headword index → SpellCheck" feed. NEW `detectors/union_attestation.py`
  loads SanskritLexicography's `HeadwordLists/union/union_headwords.tsv` (323k SLP1 headwords + n_dicts,
  sibling repo, NOT committed here — env `SANSKRIT_UNION_HEADWORDS` / graceful absence like
  get_external_source). `run_all.py` tags each candidate `union=N` and **demotes broadly-attested
  suspects (≥5 dicts) A→B** (never below B, never for high-precision flaggers, never the whitelist).
  **Honest finding:** at threshold 5 the tier-A FP reduction is small — tier-A suspects are almost all
  union 0 (2167) or 1–2 (3005); only 197 at 3–4, **0 at ≥5** before demotion (just 2 demoted). The
  per-candidate `union=N` tag is the more useful product; threshold is a maintainer tunable. Degrades
  to no-op when the index is absent (verified: identical candidate counts).
- **Hypothesis ledger + use cases** (changelog Unreleased): NEW `docs/HYPOTHESES.md` — 7 confirmed /
  4 refuted / 7 open hypotheses with evidence (body=ground-truth; tier-A precision split; do-not-file
  = deliverable; drift=f(reform type); metric dates the epoch; corpus-norm+freq=clean pairs; ext-src
  shim || REFUTED: tier-C corpus promotion, vidyut promoter, re-run recall, base-TEI norm). USE_CASES
  §11–§14 (triage, external src, ortho-drift, reform-pair harvest) + README/findings pointers.
- **DTA long-tail merge (Task 4)** (changelog Unreleased): German reform map **2,823 → 15,685 forms**.
  `detectors/extract_dta_pairs.py` (NEW) streams the DTA lingattr-TEI zip (`external_src/dta/`,
  gitignored; `<w norm="X">surface</w>` CAB layer), harvested 596k surface≠norm pairs, kept ≥20×
  (43,579), merge_reform_pairs dic-validated → +12,862 clean reform pairs (vnd→und, Theil→Teil…).
  ≥20× threshold drops OCR singletons (aaal→all). DTA zip+TSV gitignored; only de_reform_map.tsv
  committed. ⚠️ Download was flaky (Windows schannel TLS resets) — resume-loop `curl -C -` + skip the
  ~20 corrupt zip members. de_DE validation = Adobe dic (103k) auto-resolved.
- **Tier-C calibration (Task 2) — NEGATIVE RESULT** (changelog Unreleased): corpus+confusion tier-C→B
  promotion (sugg band≥4 + suspect band0 + cw≥0.05) lifts 602 dict_vs_corpus-alone C→B but surfaces
  **real minimal pairs as typos** (patra=leaf vs pAtra=vessel; vata/rAtrI = real MW headwords) —
  vowel-length needs the body; suspect_band==0 unreliable (DCS gaps). Corpus-attested pairs (band≥3)
  already tier B; C-stuck known pairs are 99% spell_correct band0-2. **Rejected tier promotion**, kept
  a documented CORROB_* score nudge (within-tier rank only). Tiers unchanged, FP=0, 26 tests. ⚠️ DON'T
  re-attempt corpus-based tier-C promotion — it's a ceiling (the body-grounded triage is the arbiter).
- **Ortho-drift within-EN recency control** (changelog Unreleased): registered PD/PE/BHS/IEG/VEI as
  `en` in `ortho_drift.py` LANG_OF; en_GB ref = ropensci/hunspell `en_GB.dic` staged at
  `external_src/hunspell/` (gitignored; `_dic()` falls back there when Adobe bundle absent). **PD
  (1976–2009, 1.32M tokens) = 0.00 drift/1k**, PE/BHS/IEG 0.00, VEI 0.06 (æ ligatures). Confirms the
  recency gradient WIL 0.57→MW 0.01→modern 0.00; the metric dates orthography. Findings doc updated.
- **Campaign mode regression fix** (changelog Unreleased): restored documented
  `detectors/run_campaign.py` workflow after the `score_tier(..., stems)` API change; added a
  regression check proving campaign rows carry `band` and `best` in the `run_all.write_review_html`
  order.
- **PD triage on source 1 — ALL 33 dicts done** (changelog 1.40.0): Deccan College Encyclopaedic
  Dict, 1007 tier-A → 0 fileable, 888 realword, **116 do-not-file (richest of any dict: 66 v.l. /
  16 w.r. / 22 xref / 12 other)**. PD records non-standard attested spellings on purpose w/ citations
  → filing corrupts the record. 1 typo-unsure (akzAMsa) source-refuted; 0 unlocatable. Suppress →
  33 dicts / 2297 unique; run_all --rerun + eval FP=0. 2nd PD source still expected (re-run refines only).
- **PD external-source wiring** (changelog 1.39.0): PD (Deccan College Encyclopaedic Dict, en) was
  the one csl-listed dict NOT in csl-orig → blocked. Added `triage_util.source_file()` (external_src/
  override → csl-orig fallback; csl_dict_file + build_entry_index route through it; 25/25 tests, MW
  unchanged), `detectors/get_external_source.py` (fetch+unzip pdtxt.zip → external_src/pd/pd.txt,
  55MB/107630 entries; has a 2nd-source slot), gitignored external_src/, registered PD=en. Verified
  buildable (1007 tier-A, 920 to-judge, 0 unlocatable). Triage RUN held for PD's 2nd source. License
  CC BY-NC-SA → source never committed here. Docs: corrections_draft/PD/readme.md + CLAUDE.md note.
- **BUR + STC + BOP triage** (changelog 1.38.0): the 3 cross-language dicts (fr/fr/la), all **0
  fileable** — 32/33 dicts done, only PD left. BUR 20 / STC 9 / BOP 6 do-not-file. The fr/la
  profiles (added earlier, uncommitted) now have test coverage (test_triage.py → 25 checks); the
  tests surfaced a missing STC `lire <form>` wrong-reading marker → added `\blire\s+[{˚]`. Suppress
  list → 32 dicts / 2181 unique; `run_all.py --rerun` + `eval.py` FP=0 all 4 correctors. All Opus
  review gates held back the b/v + vṛddhi candidates correctly (none survived to FILE-FIRST).
- **VEI + MCI + PGN + KRM triage** (changelog 1.37.0): four small dicts. **MCI 10 fileable** (headword
  keying errors contradicted by the entry's OWN citations — Mahānadi/mahānadī etc.; confirm+review,
  source-verified). VEI 0/2dnf, PGN 0/1dnf, KRM 0/6dnf (ṇ/ṣ-upadeśa roots = intentional). **29/33
  dicts; 122 fileable across 11.** Suppress 29 dicts, eval FP=0. Remaining: BUR/STC (fr), BOP (la) —
  need fr/la profiles in triage_lang; PD blocked (external sources).
- **INM + PE triage** (changelog 1.36.0): two proper-name indices, both **0 fileable**. INM
  (Sörensen Mbh names, en): 16 do-not-file, 123 entities, 12 unloc. PE (Mani Purāṇic Encyclopaedia,
  en): 13 do-not-file, 138 entities. **25/33 dicts.** Suppress 25 dicts, eval FP=0.
- **IEG triage** (changelog 1.35.0): Sircar Indian Epigraphical Glossary (en). 162 tier-A →
  **0 fileable**, 40 do-not-file (xref 21, other 19), 101 attested epigraphic terms, 3 unlocatable.
  IEG records inscriptional spellings (Prakrit doubling, retroflex/dental shifts) + cross-refs
  Prakrit→Sanskrit. **23/33 dicts.** Suppress 23 dicts, eval FP=0.
- **ACC triage** (changelog 1.34.0): Aufrecht Catalogus Catalogorum (en). 174 tier-A → **22 fileable**
  (dropped-ā in work-titles, vr→b, retroflex ḍ, sibilant/aspirate), 68 real, 25 do-not-file, 25 eyes,
  4 typo-unsure. Confirm+Review subagent w/ explicit DIRECTION check — excluded reversed pair
  aBijYAnaSAkuntala (already correct) + redirects/vrddhi. **22/33 dicts; 112 fileable across 10.**
  ⚠️ catalogue = faithful colophon spellings; FILE-FIRST DRAFT, scan-verify before filing.
- **YAT triage** (changelog 1.33.0): Yates Skt-Eng (1846, Bengal). **HIGH-YIELD: 27 fileable/247
  (10.9%)** — 2nd after SHS. Ran full Confirm+Review via subagents (Workflow tool absent). 32 b↔v
  pairs HELD for scan (Bengali व/ब undistinguished → may be faithful print, NOT filed). 123 real
  roots, 13 eyes, 15 unlocatable, 1 do-not-file. Fileable classes: n/R, sibilant, aspiration,
  vowel-length, each fixed by the entry's own citation form. **21/33 dicts; 90 fileable across 9.**
  ⚠️ FILE-FIRST is DRAFT — human must scan-verify each before filing to CORRECTIONS.
- **PUI triage** (changelog 1.32.0): Purāṇic Index (proper names, en). 518 tier-A → **0 fileable**,
  21 do-not-file, 467 real named entities, 6 unlocatable. Same 4-subagent flow. Registered all
  remaining index dicts in _LANG at once (PUI/INM/PE/YAT/ACC/IEG/MCI/PGN/VEI=en, KRM=sa). Suppress
  2020→2041, eval FP=0. **20/33 dicts.** Name indices = ~all REALWORD (names in own spelling).
- **BHS triage** (changelog 1.31.0): Edgerton's Buddhist Hybrid Sanskrit Dict (registered BHS=en in
  triage_lang). 713 tier-A → **0 fileable**, 294 do-not-file, 415 real words, 4 unsure. Workflow
  tool absent this session → classified the 19 batches (000-018) via 4 spawned subagents + did batch
  000 by hand; all 0 TYPO so no confirm/review pile. Suppression list 1726→2020 unique (19 dicts),
  eval FP=0. 19/33 dicts. Expected near-zero precision (specialized hybrid-Sanskrit lexicon: hybrid/
  MIndic/m.c. forms + proper names). ⚠️ batches are ZERO-indexed (000..N-1) — don't skip 000.
- **Ortho-drift findings write-up** (changelog 1.30.0): `docs/ORTHO_DRIFT_FINDINGS.md` — standalone
  citable synthesis of the 5-language study for a lexicography/DH audience (headline, method,
  per-lang tables, SCH-1928 control, caveats, reproducibility). All numbers verified vs the committed
  `ortho_drift/*.tsv`/`*_drift_report.txt` (reform_map sizes de 2823/en 71/fr 18/la 0/ru 7709;
  Kossovich 31389/358.17; PW 8683/697 forms; SCH flip th 76 / ß 319). Doc-only. Task A of H008-Opus_SanskritSpellCheck_NEXT_23.06.26.md.
- **Do-not-file suppression layer** (changelog 1.29.0): wired the durable triage deliverable into
  the detectors. `detectors/gen_do_not_file_suppress.py` collects the left-column headwords from all
  18 `corrections_draft/<DICT>/<DICT>_wrong_readings.txt` → `nochange/do_not_file_suppress.txt`
  (**1726 unique** of ~1976; rest cross-dict dups). `slp1util.load_whitelist()` now unions
  nochange.txt with the sibling suppress file (provenance kept separate); `run_all.py` aggregate
  filters the whitelist at the single choke point so the FLAGGERS (phonotactic/charset) are covered
  too — caught the charset-flagged Vedic `arI|a` "not licked" (`|` is a legit SLP1 char). VERIFIED:
  eval.py FP=0 all 4 correctors; 0/1726 survive in combined_candidates.txt (15377→15323); no BOM.
  ⚠️ Suppressed ONLY the *_wrong_readings.txt (do-not-file) headwords, NOT *_file_first_sf.txt (real
  typos pending CORRECTIONS filing). Regenerate the suppress file after triaging more dicts.
- **Ortho-drift: external reform-pair merge tool** (changelog 1.28.0): `detectors/merge_reform_pairs.py`
  ingests any external old→new pair file into `<lang>_reform_map.tsv`, **dic-validated** (accept iff
  old∉dic & new∈dic) → filters hallucinations/dual-spellings/rejected-proposals. ⚠️ WebFetch DOES reach
  the web (pip's PyPI timeout was its own issue) but CAN'T bulk-download DTA/RIDGES corpora (it
  summarises via a small model). First input: **14 documented 1901/1996 pairs** harvested from German
  Wikipedia reform articles (`de_reform_web_candidates.tsv`) → **de map 2809→2823** (cigarre→zigarre,
  guitarre→gitarre, liqueur→likör, schloß→schloss, rauh→rau…). The tool is the ready wiring for true
  DTA/RIDGES — drop a local export / give a row-list URL to merge the long tail.
- **Ortho-drift Phase 2: English (10-dict cluster)** (changelog 1.27.0): added `en` profile
  (ref=en_GB so British honour/-ise/-re not flagged; tokenizer strips `<s>..</s>` Sanskrit which
  MW-family use). Ran MW/MW72/AP/AP90/WIL/BEN/GST/CAE/MD/SHS. English drift = convention,
  editor/age-dependent: **WIL 1832 = 0.57/1k max** (Johnsonian -ick garlick/musick + æ æther/chamæleon
  + reflexion), MW 1899 ≈ 0.01, AP/CAE = 0; range 0.00–0.57. **COMPLETES 5-LANG STUDY. Headline:
  drift magnitude = f(reform TYPE): legislated (RU 358, DE 10) ≫ convention (EN ≤0.57, FR ≤0.31) ≫
  none (LA 0) — 1–3 orders of magnitude.** en_drift_summary.tsv + 71-form en_reform_map.tsv. DE/FR/LA/RU untouched.
- **Ortho-drift: French/Latin/Russian** (changelog 1.26.0): generalized `ortho_drift.py` →
  profile-driven (de/fr/la/ru; German profile verbatim, outputs untouched). **Russian (Kossovich,
  pre-1918, ex-SamudraManthanam kossovich.jsonl): 31389 drift / 358 per 1k ≈ 36% of tokens**,
  wordlist-FREE (1918 letters ѣ/і/ѳ/ѵ + final ъ definitional) — the most sweeping reform. French
  (BUR 1866 / STC 1932): 0.31 / 0.02 per 1k — minimal convention drift (⚠️ BUR/STC use inline IAST
  not `{#..#}` → a few fragments leak). Latin (BOP 1847): **0 drift = negative control** (no reform,
  no wordlist). Drift-rate tracks reform SCOPE: RU 1918 ≫ DE 1901/96 ≫ FR ≫ LA. Per-lang
  `<lang>_reform_map.tsv`/`_drift_summary.tsv`. fr_FR Hunspell = LOCAL DEP.
- **Ortho-drift: residual classify (recall harvest)** (changelog 1.25.0): deduped the 9482 German
  residuals (PW/PWG/GRA/CCS/SCH) → **6804 unique** → classified vs Duden (39 Sonnet agents).
  **1831 reform-drift** (27%; inflected/compound the rule missed) + 2981 fragment/OCR (44%) + 1105
  Latin (16%) + 759 modern + 106 names + 22 unsure. Folded the 1831 into `de_reform_map.tsv` →
  **978 → 2809 forms**; verdicts in `ortho_drift/de_residual_classified.tsv`. modern_form advisory.
  Did NOT re-run the 5 dicts (keeps the SCH-control table stable; recall is banked in the map for
  future runs). [1.24.0] = doc-accuracy pass (981→978 etc., fact-check-driven).
- **Ortho-drift: German cluster + SCH-1928 control** (changelog 1.23.0): ran PWG/GRA/CCS/SCH --full;
  map accumulated to **978 forms**; per-dict drift-by-era → `ortho_drift/de_drift_summary.tsv`.
  **CONTROL VALIDATED**: pre-1901 dics 1901-th-dominated (PW 6203/PWG 6508/GRA 1460/CCS 341, ~no ß);
  **SCH 1928 FLIPS** — 1901-th collapses to 76, 1996-ß dominates 319 (Kuß/Bewußtsein/Mißgunst).
  Method correctly dates each dict's orthographic epoch; drift/1k declines monotonically by date
  (10.26→8.86→7.90→4.72→2.52). Added per-era summary + de_drift_summary.tsv to ortho_drift.py. DOC-ONLY.
- **Ortho-drift: reform map externalized + expanded** (changelog 1.22.0): `ortho_drift/de_reform_map.tsv`
  — ortho_drift.py loads it at startup (∪ curated seed) + folds each run's transform/dic-confirmed
  drift back in (accumulates across dicts; works without the Hunspell dic). Seeded from full PW:
  **715 forms** (366 th, 224 c, 84 iren, 21 c-iren, 12 ss, 5 ey, 3 misc) vs ~40 inline. ⚠️ DTA/RIDGES are
  ONLINE + this env has NO outbound internet (pip/PyPI unreachable) → seeded from the corpus' own
  validated drift instead; the .tsv is the expandable container to merge DTA/RIDGES pairs into when
  reachable (or drop files locally like the Hunspell dic). DOC-ONLY.
- **Ortho-drift: Hunspell wired + full PW** (changelog 1.21.0): `ortho_drift.py` loads modern
  German Hunspell de_DE (**Adobe InDesign's bundled dic, 2006, 103756 stems — LOCAL DEP via
  $ORTHO_DE_DIC, NOT committed**) and detects drift by **transform-and-check** (apply reform rule;
  accept iff transformed-form ∈ modern-dic & original ∉). Full PW (170556 entries / 845888 German
  tokens): 502882 modern-filtered; **8683 drift across 697 forms** (transform-and-check discovered 672
  beyond the seed; once folded into the accumulating map, later reports show dic 0 / map 8683 -- total
  stable, attribution migrates) + 2171 residual for LLM. Top gerathen→geraten(253), personificirt→personifiziert(191). PW German
  pervasively pre-1901, confirmed at scale. ⚠️ KEY FINDING: Adobe 1901/1996/2006 dics are MODERN
  dics differing only in the 1996 ss-rule (NOT 19th-c wordlists) → era set-diff only catches ß-reform
  → used transform-and-check instead. Degrades to map+patterns if dic absent. DOC-ONLY. NEXT:
  LLM-classify the 2171 residual → expand map (DTA/RIDGES) → PWG/GRA/CCS + SCH-1928 control.
- **Ortho-drift Phase-0 PW/German pilot** (changelog 1.20.0): `detectors/ortho_drift.py` +
  `ortho_drift/` outputs. PW sample (2509 entries / 12917 German gloss tokens) → **48 confirmed
  reform-drift in 13 forms** (Thier→Tier etc.) + 163 pattern-candidates. Reuses triage_util;
  curated reform map + recall patterns; DOC-ONLY (never edits csl-orig). LIVE FINDING: PW glosses
  embed `{%<bot>{{old->new||date|editor|github-url|}}</bot>%}` editorial-correction records →
  leaked github/editor-names/botanical-Latin; tokenizer now strips `{{}}`/`<bot>`/`<ls>` +
  case-insensitive abbrev filter (same annotations sit in the headword-triage data too).
  vs-Duden classify DONE (Sonnet): 114 of the 163 candidates = more reform-drift (75 th/27 c/12 iren)
  + 19 modern (t+h boundaries) + 15 Latin + 13 fragments + 2 names → **127 distinct drift forms in
  the sample; PW German pervasively pre-1901** (ortho_drift/PW_drift_classified.txt). Decisions:
  2026=Duden (Hunspell de_DE); sampled default; proper-noun=LLM-bucket+sigla-stoplist (capitalisation
  useless for German); documentary-now/OCR-only-could-graduate. NEXT: wire Hunspell wordlist (deterministic
  pre-filter, like OCR san-model dep) → expand map from DTA/RIDGES → rest of German cluster (SCH 1928 =
  internal control, expect 1996-drift but little 1901). See [[project_ortho_drift_study]].
- **Tier-2 triage runs** (changelog 1.19.0): the 19th-c. cluster (SHS/WIL/GST/CAE/AP90/MD/GRA/
  BEN/CCS) → **18/33 dicts triaged**. Standout = **SHS 37 fileable** (~15%, Śabda-Sāgara 1900 is
  poorly-digitised AND every entry gives an explicit `E. <etymology>`/inflection that confirms the
  correct spelling → method's ideal case; scrutinised all 37, each entry-contradicted — b/v, w/W,
  vowel-length, sibilant). WIL 3, GST 1; the other 6 = 0 (mature/small). Registered all 9 in
  `_LANG` (one edit). Cumulative: **63 fileable across 8 dicts, ~1,976 do-not-file**. KEY LESSON
  reconfirmed: precision is near-zero on mature dicts EXCEPT poorly-digitised sources (SHS 15%,
  PWG 2.4%) where the entry's own etymology makes each error high-confidence. ⚠️ SHS 37 is the
  largest file-first set — flag for the human to prioritise scan-verification.
- **Tier-1 triage runs** (changelog 1.18.0): SKD/AP/MW72/SCH → **9/33 dicts triaged**. New
  fileable typos: **SKD 3** (hitAbalI/pUzaBAzA/vfzaBAzA, each contradicted by its own vyutpatti);
  AP/MW72/SCH = **0** (mature/well-curated). Registered the 4 in `triage_lang._LANG`
  (SKD/SCH→sa/de, MW72/AP→en — one-line edit). Opus review gate pulled every confirmed typo in
  AP/MW72/SCH + mahotka in SKD → validated across Sanskrit/English/German. Also genericized the
  last hardcoded "MW" in synthesize (triaged.txt header + bucket-5/6 titles); PWG regenerated
  (header-only); **PW/VCP NOT re-synthesized** — their on-disk triage_work diverged from the
  committed run (newer de markers + post-review logic) so re-synth regressed them → reverted.
  ⚠️ LESSON: a committed package's gitignored triage_work may be a LATER (stochastic) re-run, not
  the run that produced the commit — re-synth only after `git diff` proves header-only.
- **Code-review cleanup refactor** (changelog 1.17.0). Behavior-preserving dedup of the triage
  pipeline (the 3 lower-severity findings: duplicated boilerplate, magic numbers, hardcoded
  language config). `triage_util.py` is now the stdlib-only shared core for paths
  (`HERE`/`ROOT`/`GITHUB`, `csl_root()`/`csl_dict_file()`, `package_dir()`/`work_dir()`), CLI
  (`reconfigure_stdio()`, `dict_arg()`), and the tunables every step must agree on
  (`BATCH_SIZE`, `INTENTIONAL_KINDS`, `NEEDS_JUDGMENT`, `SCAN_URL`). The csl-orig path was being
  reached three different ways across files -- now one. Classifier thresholds + display widths
  named; `triage_lang.subtype_order()` is the single source for the wrong-readings grouping;
  dead `re`/`glob`/`sys` imports dropped. ⚠️ A removed `ROOT` left one orphan reference in
  `make_dict_package.py:74` that `py_compile`/import could NOT catch (only a runtime NameError) --
  caught by an explicit grep for orphaned names; LESSON: after deleting a module-level binding,
  grep every removed name across the file, don't trust compile. VERIFIED: 17 tests +
  byte-identical PWG re-synth + full feeder chain re-run on throwaway AP90 (no committed file
  touched).
- **Code-review correctness fixes** (changelog 1.16.0). Recall-focused multi-agent review
  of the triage pipeline surfaced a silent-failure chain in the Opus review gate: a confirmed
  typo with no review verdict (missing/unloadable `body_review_*.json`) was filed to FILE-FIRST
  without a warning, and `load_verdicts`' `except: pass` swallowed any load error — so the
  false-positive gate could no-op invisibly. Fixed both fail-loud (stderr WARNINGs in
  `triage_synthesize.py` + `triage_util.load_verdicts`). Also: non-greedy JSON fallback in
  `load_json_array` (greedy `\[.*\]` over-captured prose with stray brackets → now scans `[`
  with `JSONDecoder.raw_decode`), `EntryIndex.bodies()` now mirrors `first()`'s k1→k2 fallback
  (k2-only headwords were silently mis-classified `missing`), and unguarded `open()` reads use
  context managers. `test_triage.py` 15→17 (k2-fallback + prose-JSON). Behavior-preserving:
  PWG re-synthesizes to the same 12 fileable / 248 do-not-file. Lower-severity findings
  (duplicated boilerplate, magic numbers) left as-is.
- **Opus-pinned Review phase** (changelog 1.15.0). 4th workflow phase (after Confirm): adversarial
  false-positive gate over the confirmed TYPO pile, pinned to revModel=**opus** regardless of
  session — so the highest-judgment step (drop vṛddhi/variant/apparatus/redirect/realword) is always
  Opus, not session-dependent. synthesize consumes body_review (excludes from FILE-FIRST + auto-comments
  reviewed-out in the sf); driver emits revModel; skill step 4 → spot-check; auto-curates what I did by
  hand for PWG. VALIDATED by resuming the PWG run (classify/confirm cached) → automated review
  reproduced my manual curation EXACTLY (kept 12, reviewed out dASaSiras+ketunAlin). SNP triaged as the
  validation dict (4 tier-A, 0 fileable) → 5 of 33 done. Pipeline is now Discover(Sonnet)→Classify(Sonnet)
  →Confirm(Opus)→Review(Opus).
- **PWG hybrid triage** (changelog 1.13.0). Large Sanskrit-German Petersburg. HYBRID model
  split: Sonnet 4.6 classified 306 realword, Opus 4.8 source-confirmed, human reviewed all 14.
  **12 fileable typos of 497 tier-A** (mostly b/v व/ब + vowel-length, each confirmed by the
  entry's OWN derivation/citation, e.g. arTavanDa quoted as lalitArTabanDaM; paRavanDa deriv
  (paRa+ba°)). **Reviewed OUT 2 false positives** the same way as PW's Richtig trap: dASaSiras
  (vṛddhi "wohl dASaSirasa von daSaSiras") + ketunAlin (attested HARIV. variant) — commented
  out in PWG_file_first_sf.txt with reasons. 248 documented-intentional (71 fehlerhaft für),
  196 real words, 2 stale. Hybrid wired in bodyaware_workflow.js (clsModel/confModel, per-phase
  pinning — NO manual toggle); fixed sf header MW's→dict-code. 4-dict fileable: MW 4 · PW 2 ·
  VCP 1 · PWG 12. ALL FOUR TARGET DICTS DONE.
- **Pipeline refactor / "improve the scripts" pass** (changelog 1.12.0), guided by a
  4-dimension multi-agent review (15 confirmed / 2 refuted, adversarially verified vs code).
  `triage_util.py` (one tolerant JSON loader + one csl-orig EntryIndex + {{Lbody}} redirect
  resolution); `bodyaware_workflow.js` (ONE canonical workflow, runtime batch DISCOVERY →
  no nbatch arg, rubric from `triage_lang.marker_hint`); `triage_dict.py` driver;
  `test_triage.py` (15 checks). Removed dead code (enrich dcs_suspect_band/known_real, the
  legacy first-pass cross-check, dup regexes). NEW marker: PW `Richtig {#X#}` correction-note
  apparatus → INTENTIONAL (don't file; caught veRatawa/helarAja/SAraRa); cross-ref markers
  made separator-independent. ⚠️ **KEY LESSON: the body-aware TYPO pass is STOCHASTIC** — re-runs
  surface a different small handful (MW 4↔0, PW 2↔0, VCP 0↔1) and re-running can LOSE genuine
  typos; so KEPT the verified committed MW(4)/PW(2) packages, only updated VCP (re-run added a
  genuine `camIkara→cAmIkara` "gold" + redirect resolution). Deterministic layers stable;
  proper recall fix = union-across-runs (future). Dropped (REFUTED): confusion-class re-rank,
  bodies-before-enrich reorder.
- **VCP triage** (changelog 1.11.0). Vācaspatyam (Sanskrit-Sanskrit). Tuned the `sa`
  profile in triage_lang.py — key marker **`{{Lbody=N}}`** = VCP redirect (variant headword
  → canonical entry). **FINDING: 0 fileable typos of 563 tier-A**; 155 real words/roots
  (garba=√garb≠garBa "womb"; nUtra "new"≠mUtra); **408 documented-intentional, 362 of them
  `{{Lbody}}` redirects** (e.g. vrAhmaRa→brAhmaRa). Fixed a cosmetic bug: triage_synthesize
  printed `MW:` for every dict's body label → now the dict code (regenerated PW_triaged.txt).
  3-dict precision: **MW 4/1954 · PW 2/657 · VCP 0/563** — tier-A is near-zero typos on mature
  dicts; the value is the do-not-file lists + preventing bad bulk edits. PWG remains (reuses `de`).
- **MULTILINGUAL triage + applied to PW** (changelog 1.10.0). `triage_lang.py` = per-dict
  language profiles (MW=en, PW/PWG=de, VCP=sa) for the documented-intentional markers;
  `make_dict_package.py <DICT>` builds any dict's tier-A package from combined_candidates.txt.
  PW (Sanskrit-German, Böhtlingk-Roth) run end-to-end with a German rubric. **PW FINDING:
  of 657 tier-A, 2 (0.3%) body-confirmed fileable typos** (Bagama→BagaRa "Umlauf der
  Gestirne"=bhagaṇa; hemana→hEmana "Adj. von heman"); 369 real words; **255
  documented-intentional incl. 95 explicit `fehlerhaft für` wrong-readings** (PW apparatus
  denser than MW's 45); 1 stale; 30 eyes. Both file-first independently re-verified vs pw.txt.
  Committed: triage_lang.py, make_dict_package.py, multilingual edits to triage_bodies/
  synthesize/enrich, + corrections_draft/PW/ (candidates/draft/triaged/file_first/wrong_readings/
  readme). PWG/VCP remain (PWG reuses `de`; VCP needs `sa` tuning).
- **BODY-GROUNDED precision triage of MW tier-A** (changelog 1.9.0). Built 4 tools —
  `triage_enrich.py` (k2/DCS/cross-dict/confusion evidence → MW_evidence.jsonl),
  `triage_bodies.py` (headword→entry-body index from csl-orig; classifies wr/variant/xref/
  realword/thin/missing — MW's OWN text is the decisive signal a spelling detector lacks),
  `triage_body_batches.py`, `triage_synthesize.py` (6-bucket ranked queue + file_first_sf).
  Ran via two multi-agent workflows (adjudicate→adversarial-verify, then body-aware
  classify→source-confirm; ~130 agents). **FINDING: of 1954 MW tier-A, only 4 (0.2%) are
  body-confirmed fileable typos** (3 of 4 are consonant-class, not vowel-length); 1161 real
  words, 630 intentional (filing CORRUPTS MW), 11 stale, 148 need eyes. The body-aware pass
  caught false positives that spelling/memory passes had "confirmed" (marga=w.r. apparatus,
  muka="smell of cow-dung", vinAsa="noseless"). I independently re-verified all 4 file-first
  vs the csl-orig entries. Committed: the 4 scripts + MW_triaged.txt + MW_file_first_sf.txt +
  rewritten MW/readme.md (corrected the old wrong worked-examples) + changelog + .gitignore
  (triage_work/ + *_evidence.jsonl intermediates ignored). LESSON: never trust an agent's
  "X is not a word" — read the dictionary's own entry; the body is ground truth.
- **Fixed `ళ` (U+0933) regression in `slp1util.devanagari_to_slp1`** (changelog 1.8.2). The 1.8.1
  dedup's `to_slp1(deva_to_iast(s))` mis-mapped retroflex `ळ` → SLP1 `x` (vocalic ḷ) not `L`:
  `deva_to_iast` renders BOTH `ळ` and vocalic `ऌ` as IAST `ḷ` (U+1E37), collapsing the distinction
  before `to_slp1` — unrecoverable post-hoc. Fixed at source: added a direct **`deva_to_slp1`** to
  the canonical **sanskrit-util** package (py `__init__.py` + js `index.mjs`, maps derived from the
  Deva→IAST maps with the one `ळ`→`L` override; traversal mirrors `deva_to_iast`), which **closes
  sanskrit-util's CHANGELOG-deferred `deva_to_slp1` item** (the `ळ`→`L` vs `x` decision). Wired
  `devanagari_to_slp1` to call it (danda→space post-step kept). Verified: only `ळ`-containing inputs
  differ from 1.8.1 master (RV `अग्निमीळे`→`agnimILe` was `agnimIxe`; `खेळ`→`KeLa`; `ऌ`/`◌ॢ` stay
  `x`). sanskrit-util: +15 golden vectors (403→418/13 fns), +3 py & matching js unit tests, 3-way
  crosscheck + pytest(19) + js vectors/units all green. Both repos UNCOMMITTED — left for review.
  (Note: SLP1→Devanāgarī round-trip + iast_to_devanagari conjunct shaping remain deferred upstream.)
- APPLIED the engine end-to-end on MW → committed corrections_draft/MW/ (1954 tier-A candidates; 1943 located in csl-orig as updateByLine drafts; MW_candidates.txt evidence + readme). DRAFT for HUMAN review — candidates NOT confirmed: verify each vs scan (marga→mArga real; atrA/vAcas/pARI likely legit inflected/variant; prAvft→prA-vft intentional per <k2>). Do not bulk-apply.
- sanskrit-util dedup: detectors/slp1util.devanagari_to_slp1 now delegates to the canonical sanskrit-util package via detectors/sanskrit_util.py shim (mirrors WhitneyRoots' relative-path shim); removed the local _DEVA_* maps. Verified byte-equivalent incl danda→space (#13, kept as a post-step). Alphabet/char-classes stay local (sanskrit-util doesn't export them). Prompted by a parallel actor's throwaway detectors/_diff_util.py (left untracked — not mine). FOLLOW-UP (cross-repo, not done here): record SanskritSpellCheck as a consumer in sanskrit-util's SHARED_CODE.md migration queue.
- Phase 3.2 vidyut morphology gate: gen_vidyut_stems.py (205k pratipadika stems) + slp1util.load_vidyut_stems + run_all morph✓ tag/nudge (suggestion is a valid vidyut stem the suspect isn't). HONEST FINDING: weak on dict headwords (only 6.6% are pratipadikas; inflected suspects like rAjA look non-stem) — an `or morph` tier-A trigger over-promoted A 7717→11220 incl. inflection non-errors (rAjA→rAja), so demoted to a ranking nudge + informational tag (+15, 9175 tagged), NOT a tier promoter. vidyut_stems.txt gitignored (opt-in regenerate where vidyut installed; tag off if absent).
- Adversarial review of Phase 2 code (workflow, 28 agents, FULL verify this time — weekly limit had reset): 24 raw → 20 confirmed. Fixed the impactful ones: ocr_verify (#1 <img>/jpg/png scans now resolve — WIL/MD/CCS/PWKVN/FRI were wrongly NO-SCAN; #2 disk cache-read so re-runs don't re-hit the server; #3 throttle EVERY request not 1/candidate; #4 retry on URLError/timeout not just 429); slp1util (#5 edit_distance clamp to cap+1; #12 vocalic-LL matra ॣ; #13 danda ।॥ as word separators so OCR tokens don't merge; #14 freqBand via .get); make_changefiles (#7 strip k1/k2; #8/#9 homograph/one-line-per-entry note in draft header). Deferred WAI/low: #6 verify→UNCERTAIN on inflected/sandhi forms (intended triage), #10 dict-not-in-csl-orig graceful, #15 final-consonant implicit-a (correct Devanagari), #16 harmless over-gen, #17-#20 eval/combined_sf reporting minors (gated by the human review HTML). Verified offline (server rate-limited me); live fetch+render proven earlier by probe.
- Roadmap Phase 2.1 OCR pipeline: ocr_verify.py — servepdf→parse <object> PDF URL→fetch scan PDF→pymupdf text/render→closest-match verify (CONFIRM/DENY/UNCERTAIN). Verified: real PDF fetch+render (4846x6634), verify() 4/4 unit tests, slp1util.devanagari_to_slp1 (अग्नि→agni etc.). OCR step pluggable, needs tesseract+san (absent here); server 429s → RATE_S=3 + backoff, small batches / run server-side. Triage prior, human confirms.
- Roadmap Phase 2.4: run_campaign.py — per-dict campaigns; splits the unified suite into campaigns/<DICT>/{review.html,candidates.txt} + campaign_summary.txt dashboard (tier-A order: MW 1977, PD 1045, BHS 737, SCH 678, PW 657...). Reuses run_all aggregate/score/review-HTML. 33 dicts. Outputs gitignored.
- Roadmap Phase 2 (partial): 2.6 data-driven confusion weights (gen_confusion_weights.py → confusion_weights.json, a/A 41% etc.; wired into run_all ranking + slp1util.confusion_weight). 2.5 make_changefiles.py = per-dict DRAFT change-files in CORRECTIONS updateByLine format, locates source line in csl-orig, edits both <k1>/<k2> (verified k1+k2 fix, boundary-safe). Blocked: 2.1 OCR (tesseract+fetch), 2.2 dcs_full.sqlite (empty local placeholder), 2.3 GRETIL (external). Open: 2.4 per-dict campaigns.
- Roadmap Phase 1.4–1.6 (Phase 1 COMPLETE): extract_csl_hw.py (source-order headwords from raw csl-orig → charset/phonotactic/order_check on raw text; order_check caveat = deviation from sanhw collation). eval.py = recall vs 3884 o_vs_O pairs (50.6% union, +15152 new), 0 false positives vs 30k known-good, tier dist of recovered (A809/B245/C913 → don't discard C), spotcheck_sample.txt (top-100 tier-A). Verify phase of the run_all adversarial review was cut by the weekly agent limit; findings self-assessed, 3 real fixes applied (d0ac0d9).
- Roadmap Phase 1.1–1.3: detectors/run_all.py unified runner — runs all detectors, dedups across them by suspect, scores + A/B/C tiers (cross-detector agreement = main signal), emits combined_candidates.txt + combined_sf.txt + combined_review.html (accept/reject UI, scan links, localStorage, export to :y/:n). sanhw1: 17098 deduped, 7618 multi-detector, A/B/C ≈ 7.7k/4.7k/4.7k. Outputs gitignored.
- DCS corpus grounding: vendored detectors/dcs_lemma_summary.json (83k SLP1 lemmas + freq bands 1-5, DCS-2021 CC-BY via VisualDCS). slp1util gains load_dcs_lemmas/normalize_lemma (DCS join key)/confusion_candidates (shared). spell_correct DCS-ranked + suppresses attested-lemma headwords (9173, 4001 suppressed, 704 band≥4); consensus 8918→7548 and intra_dup 10443→8945 (DCS suppressor). New dict_vs_corpus detector (collective errors; lowest-precision, honestly labeled).
- detectors/ package: 6 new spell-check algorithms grounded in the real error distribution (o_vs_O: vowel-length 75%/asp 13%/sib 8%/diph 4%; CORRECTIONS: v/b, ṛ/ri, encoding, dups, misorder, anti-sandhi). Shared slp1util.py (confusion model folds 99.2% of o_vs_O pairs to one key; sanskrit_sort_key matches sanhw collation, 0 violations on the sorted 431k). #1 spell_correct (noisy-channel vs MW/PW/VCP+corpus, 9921/906 corpus-corrob), #2 consensus (N-way voting, 8918), #3 intra_dup (self-contradiction, 10443), #4 phonotactic (60, 0 FP), #5 charset (28), #6 order_check (selftest+verified). Flaggers emit faultfinder X:CODE=Y:D (html/triage), correctors emit DICT:wrong:right:n (#154). All py_compile clean; outputs gitignored.
- Authored repo-specific `CLAUDE.md` (architecture + verified runtime/porting status).
- Ported all Python 2 scripts to Python 3.14 (chg_nchg_sep, ngram/ngramspellcheck, o_vs_O/sortlen, sanhw1, sanhw2); py_compile clean; runnable ones executed & reproduce expected output.
- Fixed faultfinder3a.php for clean PHP 8: preg_split null→-1; for-loop → foreach (fixes warnings AND the tail-drop coverage bug, verified additive on VCP).
- Generated `AllvsSKD/` (31959 suspects) via the full 3-step PHP 8 pipeline, warning-free.
- Built `triage_suspects.py` (signal vs noise via sanhw2.py san_spc_dicts; + post-repha gemination subset) + 2026 review packages: MW 110→17→5, PW 183→58→6, PWG 256→107→8 (in Allvs_2026/{MW,PW,PWG}/), all warning-free.
- MW72 base (medium, 51k keys → 15402 raw, noisy): kept only the gemination subset (9) + raw, in Allvs_2026/MW72/. Showed length-sort fails on small bases; post-repha doubling is the precision filter.
- Fixed faultfinder3a-html.php repeat=2 (rCC mode) — it never stored/destructured records (dead since written). Now renders rcc words. triage_suspects.py now also emits -priority.txt (non-rcc = verify-first) alongside -gemination.txt. Built clickable priority+gemination reports for MW/PW/PWG (+MW72 gemination); dropped empty -gemination-table.html files.
- REORDERED priority: non-rcc anomalies = verify-first; post-repha (rcc) = low priority (the generator's rcc() excludes them by default as faithful print). Earlier "verify gemination first" was backwards.
- Code-review (xhigh) fixes: ngramspellcheck.py stdout/stderr→utf-8 (console crash guard + project convention); triage_suspects.py parse() guards malformed lines (returns None), derive() rewrites only the filename token (rpartition not replace-all), empty-dict lines fall to signal not noise, malformed-line count reported, docstring corrected re has_gemination(Y) vs rcc(whole-word). Verified: re-running triage is byte-identical (no regression).
