# Project Objective: Modernize SanskritSpellCheck to run on Python 3 + PHP 8, keep generating dictionary suspect-lists, and refine the detectors.

## ➡️ Next Steps (Queue)
- Optionally re-run faultfinder with a *large* clean base (MW72/SHS/WIL) for a higher-precision new suspect list; SKD-as-base (small) yields 29k mostly-low-probability flags.
- Optional cleanup: `codecs.open()` → `open(encoding=…)` across the .py files (cosmetic DeprecationWarnings only).
- Consider fixing the latent for-loop tail-drop in faultfinder3a.php (the by-index loop stops at count($fileb), never visiting the last ~#whitelist survivors) — this is a *behavior change* (more suspects) so do it deliberately, not silently.
- Verify ported `sanhw1.py`/`sanhw2.py` against the real Cologne scan trees (server-side only).
- Feed confirmed SKD suspects to sanskrit-lexicon/CORRECTIONS.

## 🚧 Current Work-In-Progress (WIP)
- (none)

## 🧠 Dev Notes & Hypotheses (Bugs, ideas, context)
- Runtimes: PHP 8.2.12 at `C:\xampp\php\php.exe` (not on PATH), Python 3.14.4, lxml 6.1.0.
- **Key insight**: re-running MW/PW/PWG now gives 0/3/11 suspects vs committed 1705/1853/1984 — the 2014–2017 corrections were applied upstream and sanhw1.txt regenerated, so those dicts are clean now. NOT a bug. VCP (done last) still reproduces 6856 byte-identical → proves the PHP 8 port is faithful.
- faultfinder3a.php PHP 8 fixes: `preg_split(...,null,...)`→`-1`; `if(!isset($file1[$j]))continue;` to skip array_diff key-gaps (was flooding warnings; output unchanged — VCP 6856=6856, 0 warnings).
- `sanhw*.py` need sibling `<CODE>Scan/<year>/pywork` trees absent locally → ported-for-correctness, not runtime-verified here.

## ✅ Completed (Recent only)
- Authored repo-specific `CLAUDE.md` (architecture + verified runtime/porting status).
- Ported all Python 2 scripts to Python 3.14 (chg_nchg_sep, ngram/ngramspellcheck, o_vs_O/sortlen, sanhw1, sanhw2); py_compile clean. Runnable ones executed & reproduce expected output (sortlen byte-identical to committed output3; ngram 25 hits; chg_nchg 716 lines).
- Fixed faultfinder3a.php for clean PHP 8 (verified VCP byte-identical, warning-free).
- Generated `AllvsSKD/` (29411 suspects) via the full 3-step PHP 8 pipeline — new base dict.
