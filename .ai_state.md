# Project Objective: Modernize SanskritSpellCheck to run on Python 3 + PHP 8, keep generating dictionary suspect-lists, and refine the detectors.

## ➡️ Next Steps (Queue)
- Human review of MW signal list (17) in Allvs_2026/MW/AllvsMW-signal.txt → verify against scans → CORRECTIONS. Mostly -yya-/-rdd-/-jja-/-rmma- geminations.
- Build the same review package for PW (183 raw) and PWG (256 raw): run the 3-step pipeline into Allvs_2026/{PW,PWG}/ then `python triage_suspects.py`.
- Optionally re-run faultfinder with a *large* clean base (MW72/SHS/WIL) for a higher-precision new suspect list; SKD-as-base (small) yields ~32k mostly-low-probability flags.
- Optional cleanup: `codecs.open()` → `open(encoding=…)` across the .py files (cosmetic DeprecationWarnings only).
- Verify ported `sanhw1.py`/`sanhw2.py` against the real Cologne scan trees (server-side only).

## 🚧 Current Work-In-Progress (WIP)
- (none)

## 🧠 Dev Notes & Hypotheses (Bugs, ideas, context)
- Runtimes: PHP 8.2.12 at `C:\xampp\php\php.exe` (not on PATH), Python 3.14.4, lxml 6.1.0.
- **Tail-drop bug (now fixed)**: the check loop was `for ($j<count($file1))` over `array_diff()` output, whose keys are gappy → it stopped at the survivor count and never tested survivors past it (the alphabet tail). `foreach ($file1 as $j=>$value)` fixes both the gaps (warnings) and the coverage. Purely additive: VCP 6856→7411 (+555), all new ones s…/h…; 0 lines lost.
- **Re-run vs committed**: fresh MW/PW/PWG = 110/183/256 vs committed 1705/1853/1984. Head of alphabet was corrected upstream since 2017; the tail counts are NEW (never tested before the fix). Not a regression.
- `sanhw*.py` need sibling `<CODE>Scan/<year>/pywork` trees absent locally → ported-for-correctness, not runtime-verified here.
- Historical `AllvsMW/PW/PWG/VCP` left as 2017 point-in-time runs (audit trail) — not overwritten.

## ✅ Completed (Recent only)
- Authored repo-specific `CLAUDE.md` (architecture + verified runtime/porting status).
- Ported all Python 2 scripts to Python 3.14 (chg_nchg_sep, ngram/ngramspellcheck, o_vs_O/sortlen, sanhw1, sanhw2); py_compile clean; runnable ones executed & reproduce expected output.
- Fixed faultfinder3a.php for clean PHP 8: preg_split null→-1; for-loop → foreach (fixes warnings AND the tail-drop coverage bug, verified additive on VCP).
- Generated `AllvsSKD/` (31959 suspects) via the full 3-step PHP 8 pipeline, warning-free.
- Built `triage_suspects.py` (signal vs noise via sanhw2.py san_spc_dicts) + MW review package in `Allvs_2026/MW/` (110 → 17 signal / 93 noise).
